#foreach($ass in ${type.getManyToMany()})

    /**
     * Get the ${ass.name}List of the ${type.name}
     * @return ${ass.name}List
     * @see List
     * @see ${ass.entity.name}Proxy
     */
    public List<${ass.entity.name}Proxy> get${util.capitalize(${ass.name})}List() {
        if(${ass.name}List == null) {
            ${ass.name}List = new ArrayList<${ass.entity.name}Proxy>();
        }
        return ${ass.name}List;
    }

    /**
     * Set the ${ass.name}List of the ${type.name}
     * @param ${ass.name}List The ${ass.name} list
     * @see List
     * @see ${ass.entity.name}Proxy
     */
    public void set${util.capitalize(${ass.name})}List(List<${ass.entity.name}Proxy> ${ass.name}List) {
        this.${ass.name}List = ${ass.name}List;
    }


    /**
     * Add a ${ass.entity.name}Proxy to the ${ass.name}List of the ${type.name}
     * @param ${util.uncapitalize(${ass.name})} The ${ass.entity.name}Proxy to add
     * @see ${ass.entity.name}Proxy
     */
    public void add${util.capitalize(${ass.name})}(${ass.entity.name}Proxy ${util.uncapitalize(${ass.name})}) {
        get${util.capitalize(${ass.name})}List().add(${util.uncapitalize(${ass.name})});
    }
#end